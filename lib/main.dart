import 'package:flutter/material.dart';

void main() {
  runApp(const Rootally());
}

class Rootally extends StatelessWidget {
  const Rootally({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
